# Projects

A C programming project.

## Building

### Prerequisites
- `gcc` (GNU C Compiler)
- `make`

### Build Commands

```bash
# Build the project
make

# Run the program
make run

# Clean build artifacts
make clean
```

The compiled binary will be located in `bin/program`.